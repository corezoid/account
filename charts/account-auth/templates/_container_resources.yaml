{{- define "account.auth.resources" }}
{{- if .Values.global.account.auth.resources }}
{{- $resources := .Values.global.account.auth.resources }}
resources:
{{ $resources | toYaml | trim | indent 4 }}
{{- else }}
resources:
  limits:
    memory: 400Mi
  requests:
    cpu: 50m
    memory: 200Mi
{{- end }}
{{- end }}
